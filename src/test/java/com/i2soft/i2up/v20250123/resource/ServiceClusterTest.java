package com.i2soft.i2up.v20250123.resource;

import com.i2soft.common.Auth;
import com.i2soft.http.I2Rs;
import com.i2soft.http.I2softException;
import com.i2soft.http.Response;
import com.i2soft.resource.v20250123.ServiceCluster;
import com.i2soft.util.Configuration;
import com.i2soft.util.StringMap;
import org.junit.Assert;
import org.junit.BeforeClass;
import org.junit.FixMethodOrder;
import org.junit.Test;
import org.junit.runners.MethodSorters;
import com.i2soft.i2up.util.TestConfig;

import java.util.Map;
import java.util.Objects;
import java.util.UUID;

@FixMethodOrder(MethodSorters.NAME_ASCENDING)
public class ServiceClusterTest {

    private static Auth auth;
    private static ServiceCluster serviceCluster;

    @BeforeClass
    public static void setUp() {
        if (serviceCluster != null) {
            return;
        }
        try {
            auth = Auth.token(TestConfig.addr, TestConfig.user, TestConfig.pwd, TestConfig.cachePath, new Configuration());
        } catch (I2softException e) {
            e.printStackTrace();
        }
        serviceCluster = new ServiceCluster(auth);
    }

    @Test
    public void T3140_createServiceCls() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3140")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            I2Rs.I2SmpRs rs = serviceCluster.createServiceCls(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3141_modifyServiceCls() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3141")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            I2Rs.I2SmpRs rs = serviceCluster.modifyServiceCls(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3170_deleteServiceCls() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3170")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            Map rs = serviceCluster.deleteServiceCls(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3142_describeServiceCls() {
        try {
            String uuid = UUID.randomUUID().toString().toUpperCase();
            Map rs = serviceCluster.describeServiceCls(uuid); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3143_listServiceCls() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3143")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            Map rs = serviceCluster.listServiceCls(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3189_listServiceClsStatus() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3189")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            Map rs = serviceCluster.listServiceClsStatus(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3307_chkServiceClsNode() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3307")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            Map rs = serviceCluster.chkServiceClsNode(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3625_configServiceCls() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3625")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            I2Rs.I2SmpRs rs = serviceCluster.configServiceCls(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3626_describeServiceClsConfig() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3626")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            Map rs = serviceCluster.describeServiceClsConfig(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }

    @Test
    public void T3640_listServiceClsValidNode() {
        try {
            Response r = auth.client.get(String.format(TestConfig.rapDataUrl, "3640")); // 获取请求数据
            StringMap args = new StringMap().putAll(Objects.requireNonNull(r.jsonToMap())); // 填充请求数据
            Map rs = serviceCluster.listServiceClsValidNode(args); // 发送请求
            Assert.assertNotNull(rs); // 检查结果
        } catch (I2softException e) {
            e.printStackTrace();
        }
    }
}